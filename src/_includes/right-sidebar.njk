{# ================================================================
   Right Sidebar (Template Layer - Component)
   TOC, site stats, categories, and tag cloud.
   ================================================================ #}
<aside class="right-sidebar">
  <!-- ============================================================ -->
  <!--  Table of Contents                                            -->
  <!-- ============================================================ -->
  <div class="sidebar-card toc-card" id="tocCard" style="display:none;">
    <h3 class="sidebar-card__title">
      <i class="fas fa-list" style="margin-right:4px;font-size:0.85em;"></i>
      目录
    </h3>
    <nav class="toc" id="toc"></nav>
  </div>

  <!-- ============================================================ -->
  <!--  Site Stats                                                   -->
  <!-- ============================================================ -->
  <div class="sidebar-card site-info-card">
    <h3 class="sidebar-card__title">
      <i class="fas fa-chart-simple" style="margin-right:4px;font-size:0.85em;"></i>
      站点统计
    </h3>
    <div class="site-info-card__stats">
      <div class="site-info-card__stat">
        <span class="site-info-card__stat-value">{{ collections.posts.length }}</span>
        <span class="site-info-card__stat-label">文章</span>
      </div>
      <div class="site-info-card__stat">
        <span class="site-info-card__stat-value">{{ collections.categories.length }}</span>
        <span class="site-info-card__stat-label">分类</span>
      </div>
      <div class="site-info-card__stat">
        <span class="site-info-card__stat-value">{{ collections.tagList.length }}</span>
        <span class="site-info-card__stat-label">标签</span>
      </div>
      <div class="site-info-card__stat">
        <span class="site-info-card__stat-value">{{ "now" | year }}</span>
        <span class="site-info-card__stat-label">建站</span>
      </div>
    </div>
    <div style="display:flex;gap:0.375rem;flex-wrap:wrap;margin-top:0.625rem;justify-content:center;">
      <span class="stat-pill">
        <span class="stat-pill__value">{{ collections.posts.length }}</span> 文章
      </span>
      <span class="stat-pill">
        <span class="stat-pill__value">{{ collections.categories.length }}</span> 分类
      </span>
      <span class="stat-pill">
        <span class="stat-pill__value">{{ "now" | year }}</span> 建站
      </span>
    </div>
  </div>

  <!-- ============================================================ -->
  <!--  Categories                                                   -->
  <!-- ============================================================ -->
  {% if sidebarCategories and sidebarCategories.length %}
  <div class="sidebar-card categories-card">
    <h3 class="sidebar-card__title">
      <i class="fas fa-folder-open" style="margin-right:4px;font-size:0.85em;"></i>
      分类
    </h3>
    <ul class="categories-list">
      {% for cat in sidebarCategories %}
        <li class="categories-list__item">
          <a href="/categories/{{ cat.slug }}/">{{ cat.name }}<span class="categories-list__count">{{ cat.count }}</span></a>
        </li>
      {% endfor %}
    </ul>
  </div>
  {% endif %}

  <!-- ============================================================ -->
  <!--  Tag Cloud                                                    -->
  <!-- ============================================================ -->
  {% if sidebarTags and sidebarTags.length %}
  <div class="sidebar-card tags-card">
    <h3 class="sidebar-card__title">
      <i class="fas fa-tags" style="margin-right:4px;font-size:0.85em;"></i>
      标签
    </h3>
    <div class="tag-cloud">
      {% for tag in sidebarTags %}
        <a href="/tags/{{ tag.slug }}/" class="tag-cloud__item" data-count="{{ tag.count }}">{{ tag.name }}</a>
      {% endfor %}
    </div>
  </div>
  {% endif %}
</aside>
